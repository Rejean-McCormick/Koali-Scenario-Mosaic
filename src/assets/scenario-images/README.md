# Final scenario image masters

Drop optional final masters here as `SCN-001.png` through `SCN-036.png`.

Contract: **1254×1254, PNG**. Do not place 418/627 generated variants here.

Run `npm run images:build` after adding or replacing a master.

---
import { getLocalizedScenarios } from '../../lib/scenarios';
export async function getStaticPaths(){const scenarios=await getLocalizedScenarios('en');return scenarios.map(s=>({params:{id:s.data.id}}));}
return Astro.redirect(`/en/uses/${Astro.params.id}/`);
